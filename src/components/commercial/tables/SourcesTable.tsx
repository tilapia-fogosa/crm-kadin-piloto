
import { 
  Table, 
  TableBody, 
  TableCell, 
  TableHead, 
  TableHeader, 
  TableRow 
} from "@/components/ui/table";

interface SourcesTableProps {
  selectedMonth: Date;
  selectedUnitId?: string;
}

export function SourcesTable({ selectedMonth, selectedUnitId }: SourcesTableProps) {
  return (
    <div>Em desenvolvimento</div>
  );
}
